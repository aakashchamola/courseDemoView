import 'package:flutter/material.dart';

class OfflineHomeScreenWidget extends StatelessWidget {
  const OfflineHomeScreenWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
